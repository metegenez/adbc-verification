---
plan: 04-03
phase: 04
title: Phase 4 Integration — run-verify wiring + REQUIREMENTS + ROADMAP + CLAUDE.md
type: execute
wave: 3
depends_on: [04-01, 04-04, 04-05]
autonomous: true
files_modified:
  - run-verify.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CLAUDE.md
requirements: []   # this plan registers, doesn't implement; cites all 9 in body
must_haves:
  truths:
    - "run-verify.py:_wait_for_healthy() blocks pytest until sr-external is healthy"
    - ".planning/REQUIREMENTS.md lists FS-SR-01..FS-SR-09 with the same row format as VAL-01..VAL-07 and traceability rows for all nine"
    - ".planning/ROADMAP.md Phase 4 entry has a real Goal, the full Requirements list (FS-SR-01..09), and 9 numbered Success Criteria — no [TBD] / [To be planned] left"
    - "CLAUDE.md tests/ listing includes test_flightsql_starrocks.py; queries/ listing includes the canonical queries/tpch/ home and removes the per-backend 03-q* duplicates"
    - "CLAUDE.md has a Phase 4 sub-section, the Retired table is deleted (since 04-04 actually removes those files), and the Phase 2 SF1 mention is updated to the DuckDB tpch generator"
    - "CLAUDE.md Pitfalls section flags DUPLICATE KEY auto-pick + run-verify.py service dict (linked to 04-RESEARCH.md, no duplication)"
  artifacts:
    - path: "run-verify.py"
      provides: "_wait_for_healthy() services dict containing 'sr-external': False"
      contains: "'sr-external': False"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FS-SR-01..FS-SR-09 requirement rows + traceability rows + updated v1 count"
      contains: "FS-SR-09"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 entry with concrete Goal, Requirements (FS-SR-01..09), 9 Success Criteria, plan list"
      contains: "FS-SR-09"
    - path: "CLAUDE.md"
      provides: "Phase 4: External StarRocks Arrow Flight section + tests/ listing entry + queries/tpch/ canonical entry + 2 new pitfall callouts; Retired table removed; Phase 2 SF1 mention re-pointed at DuckDB tpch"
      contains: "test_flightsql_starrocks.py"
  key_links:
    - from: "run-verify.py:_wait_for_healthy()"
      to: "docker-compose.yml sr-external service"
      via: "service name string match in `services` dict + `docker compose ps --format json` output"
      pattern: "'sr-external': False"
    - from: ".planning/ROADMAP.md Phase 4 Requirements: line"
      to: ".planning/REQUIREMENTS.md FS-SR-01..09 rows"
      via: "FS-SR-NN ID strings repeated in both files"
      pattern: "FS-SR-0[1-9]"
    - from: "CLAUDE.md Project Layout"
      to: "tests/test_flightsql_starrocks.py (created by 04-02) + queries/tpch/ (created by 04-05)"
      via: "filename / directory string"
      pattern: "test_flightsql_starrocks.py|queries/tpch"
---

<objective>
Land the integration scaffolding for Phase 4: wire `sr-external` into `run-verify.py`'s pre-pytest health wait loop (RESEARCH.md Pitfall 7); register nine requirement IDs (FS-SR-01..FS-SR-09) in `.planning/REQUIREMENTS.md` with the same row shape as VAL-01..VAL-07 plus traceability rows that map each ID to its implementing plan (04-01 / 04-02 / 04-04 / 04-05); fill in `.planning/ROADMAP.md` Phase 4's `Goal:` / `Requirements:` / Success Criteria placeholders so no `[TBD]` / `[To be planned]` text remains and 9 numbered Success Criteria mirror Phases 1-3; and update `CLAUDE.md` (Project Layout entry for `test_flightsql_starrocks.py` + the canonical `queries/tpch/`, a new "Phase 4: External StarRocks Arrow Flight" subsection, an updated Phase 2 SF1 paragraph that points at the DuckDB tpch extension, two pitfall callouts that link back to `04-RESEARCH.md` rather than duplicate it, and **deletion of the entire `## Retired` table** because 04-04 actually deletes the four files it lists). This plan ships traceability + docs + the runner fix; it does not implement any FS-SR-* itself — those are owned by 04-01 (FS-SR-01..03), 04-02 (FS-SR-04..06), 04-04 (FS-SR-07), and 04-05 (FS-SR-08, FS-SR-09).
</objective>

<context_summary>
- Source artifacts: `04-CONTEXT.md` (canonical decisions D-01..D-16, "What Changes" table); `04-CANONICAL-SPEC.md` (queries/tpch/ contract feeding FS-SR-09 + DuckDB tpch generator feeding FS-SR-08); `04-RESEARCH.md` Pitfalls 1 + 7 (DUPLICATE KEY auto-pick rationale, run-verify.py service dict); `04-PATTERNS.md` (exact analogs for run-verify.py edit + REQUIREMENTS.md row shape).
- Sibling plan ownership: 04-01 owns `docker/docker-compose.yml`, `docker/init/sr-external/` (FS-SR-01..03). 04-02 owns `tests/test_flightsql_starrocks.py`, `tests/test_queries.py`, `queries/tpch/` consumers (FS-SR-04..06). 04-04 owns dead-`lib/` deletion (FS-SR-07). 04-05 owns the DuckDB-tpch generator + `queries/tpch/` corpus + deletion of `queries/{mysql,postgres}/03-q*.sql` (FS-SR-08, FS-SR-09). This plan owns nothing they touch.
- Wave 3 placement is required: this plan reads CLAUDE.md after 04-04 has deleted the four retired files (so the `## Retired` table is genuinely empty/redundant) and after 04-05 has populated `queries/tpch/` and removed the per-backend `03-q*.sql` duplicates (so the Project Layout block can describe the final post-cleanup state). Without `depends_on: [04-01, 04-04, 04-05]`, this plan would describe a state that doesn't yet exist on disk.
- Cross-plan contract: 04-01 cites FS-SR-01..03 in its frontmatter; 04-02 cites FS-SR-04..06; 04-04 cites FS-SR-07; 04-05 cites FS-SR-08..09. This plan registers all nine IDs into `.planning/REQUIREMENTS.md` so `/gsd-verify-work` can match them. Per the planning-context note, requirement rows must be live by verification time, not by the implementing plans' execution time — wave 3 is fine.
- The frontmatter `requirements: []` field is intentional: this plan adds the requirement traceability rows themselves (a meta-task). It does not "deliver" any single FS-SR-NN behavior. The IDs FS-SR-01..09 are still cited inline in the body (Task 04-03-T01).
- The runner edit is genuinely a one-line dict insertion. Bracketing context is required only to land the line in the correct alphabetical-ish slot (between `sr-flightsql-tls` and `sr-main`, matching natural `depends_on` ordering per D-04).
</context_summary>

<tasks>

<task id="04-03-T01" type="auto">
  <title>Add FS-SR-01..FS-SR-09 to .planning/REQUIREMENTS.md and extend the Traceability table</title>

  <read_first>
    - `.planning/REQUIREMENTS.md` (full file — 119 lines)
    - `04-PATTERNS.md` lines 511-568 (REQUIREMENTS.md analog: existing v1 row format with multi-line `**ID\n**:` shape, plus existing Traceability table at lines 89-109)
    - `04-CONTEXT.md` decisions D-01..D-16 (the source decisions that map to FS-SR-01..06)
    - `04-CANONICAL-SPEC.md` (full file — contract for FS-SR-08 generator + FS-SR-09 canonical query home)
  </read_first>

  <action>
**Edit 1 — Insert a new subsection under `## v1 Requirements`, immediately after the `### Multi-Driver Validation (Existing — Adapted)` block (which ends at line 56 with VAL-07 `... (7 tests)` and is followed by line 57 blank, line 58 `## v2 Requirements`). Insert these lines BETWEEN line 56 (last VAL bullet) and line 57 (blank):**

```markdown

### FlightSQL → External StarRocks (Phase 4)

These cover the new `sr_flightsql_starrocks` catalog backed by an external StarRocks instance (`sr-external`) populated with TPC-H SF1 native tables, plus the cleanup work that consolidates the TPC-H corpus into a single canonical home and retires dead `lib/` files. The existing sqlflite FlightSQL path (VAL-05) coexists.

- [ ] **FS-SR-01
**: `sr-external` Compose service exists, builds from the same Dockerfile as sr-main, publishes no host ports, mounts `./data/sf1/` read-only and `./init/sr-external/` for init SQL, and reports healthy via the same `mysql -uroot -h127.0.0.1 -P9030 -e "SELECT 1"` healthcheck shape (D-01, D-02, D-03)
- [ ] **FS-SR-02
**: TPC-H schema is created in sr-external under database `tpch`, and all 8 tables (region, nation, supplier, part, partsupp, customer, orders, lineitem) are loaded from the canonical SF1 CSVs via `INSERT INTO tpch.<table> SELECT * FROM FILES('file:///opt/starrocks/data/sf1/<table>.csv', 'format'='csv', ...)` (D-05, D-07, D-08)
- [ ] **FS-SR-03
**: Init is idempotent across both cold (`down -v && up`) and warm (`restart`) boots — every init SQL file uses `CREATE DATABASE IF NOT EXISTS` / `CREATE TABLE IF NOT EXISTS` / `TRUNCATE TABLE` so re-running the init loop converges to the same row counts (D-04, RESEARCH Pitfall 8)
- [ ] **FS-SR-04
**: `tests/test_flightsql_starrocks.py` ships 4 scenarios — lifecycle, data, wrong-password, ADBC pass-through — and they all pass against a `sr_flightsql_starrocks` catalog at `grpc://sr-external:9408` with `username=root` / `password=""` (D-11, D-13, D-15, D-16)
- [ ] **FS-SR-05
**: All 22 canonical TPC-H query files under `queries/tpch/q01.sql..q22.sql` execute via `tests/test_queries.py` against the `sr_flightsql_starrocks` catalog and return correct row counts (D-09, D-10, RESEARCH Pitfall 6 — adopt mysql counts, NOT postgres counts)
- [ ] **FS-SR-06
**: The pre-existing sqlflite path coexists — `tests/test_flightsql.py` (5 tests against `sr-flightsql` / `sr-flightsql-tls`) and `queries/flightsql/` (2 query files) continue to pass with no regression after sr-external lands (D-12, D-14)
- [ ] **FS-SR-07
**: Dead/superseded `lib/` files are deleted — `lib/docker_backends.py`, `lib/starrocks.py`, `lib/tls.py`, and `lib/driver_registry.py` are removed (post-Phase-1 they're no longer imported by `conftest.py` or any test). `lib/` retains only `__init__.py` and `catalog_helpers.py`.
- [ ] **FS-SR-08
**: `docker/generate-sf1-data.py` is rewritten to use the canonical DuckDB `tpch` extension (`INSTALL tpch; LOAD tpch; CALL dbgen(sf=1); COPY <table> TO '<path>' (FORMAT CSV, HEADER TRUE, DELIMITER ',')`). Generator file is ≤50 lines, deterministic, produces LF-terminated CSVs with header rows at the canonical SF1 row counts (region=5, nation=25, supplier=10000, part=200000, partsupp=800000, customer=150000, orders=1500000, lineitem=6001215) — no hand-rolled mock data generation. Header rows are required by all consumers (postgres `\COPY ... HEADER true`, mysql `LOAD DATA ... IGNORE 1 ROWS`, sr-external `csv.skip_header='1'`).
- [ ] **FS-SR-09
**: `queries/tpch/q01.sql..q22.sql` is the single canonical TPC-H query home. The 44 per-backend duplicates `queries/mysql/03-q*.sql` and `queries/postgres/03-q*.sql` are deleted. Canonical files use `{catalog}.{db}` template substitution at test-collection time, with the per-backend skip manifest (`CANONICAL_SKIPS`) living in `tests/test_queries.py` and the per-backend mapping in `CANONICAL_BACKENDS` (per `04-CANONICAL-SPEC.md`).

```

**Edit 2 — Extend the Traceability table at line 91. Insert these 9 rows immediately after the existing `| VAL-07 | Phase 1: Docker Compose Verification Suite | 01-01 | Pending |` row (line 109):**

```markdown
| FS-SR-01 | Phase 4: FlightSQL TPC-H Queries Against External StarRocks | 04-01 | Pending |
| FS-SR-02 | Phase 4: FlightSQL TPC-H Queries Against External StarRocks | 04-01 | Pending |
| FS-SR-03 | Phase 4: FlightSQL TPC-H Queries Against External StarRocks | 04-01 | Pending |
| FS-SR-04 | Phase 4: FlightSQL TPC-H Queries Against External StarRocks | 04-02 | Pending |
| FS-SR-05 | Phase 4: FlightSQL TPC-H Queries Against External StarRocks | 04-02 | Pending |
| FS-SR-06 | Phase 4: FlightSQL TPC-H Queries Against External StarRocks | 04-02 | Pending |
| FS-SR-07 | Phase 4: FlightSQL TPC-H Queries Against External StarRocks | 04-04 | Pending |
| FS-SR-08 | Phase 4: FlightSQL TPC-H Queries Against External StarRocks | 04-05 | Pending |
| FS-SR-09 | Phase 4: FlightSQL TPC-H Queries Against External StarRocks | 04-05 | Pending |
```

**Edit 3 — Update the coverage tally at lines 111-114. Replace exactly:**

```markdown
**Coverage:**
- v1 requirements: 17 total
- Mapped to phases: 17
- Unmapped: 0 ✓
```

**With:**

```markdown
**Coverage:**
- v1 requirements: 26 total (17 Docker Compose + 9 FlightSQL→StarRocks)
- Mapped to phases: 26
- Unmapped: 0 ✓
```

**Match the existing format quirk verbatim:** the `**FS-SR-NN\n**:` line break inside the bold marker is intentional and matches DC-01..VAL-07 throughout the file. Do NOT collapse to `**FS-SR-01**:` (would render the same, but the project convention uses the multi-line shape and the analog in `04-PATTERNS.md` lines 547-548 calls this out explicitly).

**Plan-to-requirement mapping rationale:**
- FS-SR-01..03 → Plan 04-01 (sr-external Compose service + schema + init data scripts)
- FS-SR-04..06 → Plan 04-02 (test module + canonical query corpus consumer + sqlflite coexistence verification)
- FS-SR-07     → Plan 04-04 (delete dead `lib/docker_backends.py`, `lib/starrocks.py`, `lib/tls.py`, `lib/driver_registry.py`)
- FS-SR-08..09 → Plan 04-05 (rewrite generate-sf1-data.py with DuckDB tpch + create canonical `queries/tpch/` + delete `queries/{mysql,postgres}/03-q*.sql`)
- This plan (04-03) registers all nine IDs but implements none of them; that's why frontmatter `requirements: []` is empty.
  </action>

  <acceptance_criteria>
- `grep -q "FS-SR-01" .planning/REQUIREMENTS.md` succeeds.
- `grep -q "FS-SR-09" .planning/REQUIREMENTS.md` succeeds.
- `grep -q "### FlightSQL → External StarRocks (Phase 4)" .planning/REQUIREMENTS.md` succeeds.
- `grep -c "FS-SR-0" .planning/REQUIREMENTS.md` returns at least `18` (9 bullet rows + 9 traceability rows).
- `grep -c "^| FS-SR-0[1-9] |" .planning/REQUIREMENTS.md` returns `9` (nine traceability rows, one per ID).
- `grep -q "v1 requirements: 26 total" .planning/REQUIREMENTS.md` succeeds.
- `! grep -q "v1 requirements: 17 total" .planning/REQUIREMENTS.md` succeeds (old count was replaced).
- `grep -c "^\*\*FS-SR-0[1-9]$" .planning/REQUIREMENTS.md` returns `9` (multi-line bold marker preserved on each ID).
- `grep -q "Phase 4: FlightSQL TPC-H Queries Against External StarRocks" .planning/REQUIREMENTS.md` succeeds.
- `grep -q "VAL-05" .planning/REQUIREMENTS.md && grep -q "DC-01" .planning/REQUIREMENTS.md` succeeds (existing entries untouched).
- `grep "^| FS-SR-07 " .planning/REQUIREMENTS.md | grep -q "04-04"` succeeds.
- `grep "^| FS-SR-08 " .planning/REQUIREMENTS.md | grep -q "04-05"` succeeds.
- `grep "^| FS-SR-09 " .planning/REQUIREMENTS.md | grep -q "04-05"` succeeds.
  </acceptance_criteria>
</task>

<task id="04-03-T02" type="auto">
  <title>Fill in .planning/ROADMAP.md Phase 4 — Goal, Requirements list, 9 Success Criteria, plan list (5 plans), Progress row</title>

  <read_first>
    - `.planning/ROADMAP.md` lines 80-89 (current Phase 4 entry with `[To be planned]` / `TBD` placeholders)
    - `.planning/ROADMAP.md` lines 41-58 (Phase 2 entry — analog format for Goal / Requirements / Depends on / Success Criteria / Plans)
    - `.planning/ROADMAP.md` lines 59-79 (Phase 3 entry — second analog with same shape, slightly longer goal)
    - `04-CONTEXT.md` lines 6-28 (Phase Boundary — source for the goal text)
    - `04-CANONICAL-SPEC.md` (the 9th success criterion language about `queries/tpch/` as the single canonical TPC-H source)
  </read_first>

  <action>
**Edit 1 — Replace lines 80-89 of `.planning/ROADMAP.md` (the entire current Phase 4 entry, from `### Phase 4:` through the placeholder bullet `- [ ] TBD (run /gsd-plan-phase 4 to break down)`) with the following block. Match the heading levels and bold-key formatting used by Phase 2 / Phase 3 exactly:**

```markdown
### Phase 4: FlightSQL TPC-H queries against external StarRocks with Arrow Flight ports

**Goal:** Add a second StarRocks instance (`sr-external`) to the Docker Compose stack, populate its native StarRocks tables with TPC-H SF1 data via the `FILES()` table function, and verify the StarRocks-native Arrow Flight server path end-to-end. From sr-main, the new ADBC FlightSQL catalog `sr_flightsql_starrocks` connects to `grpc://sr-external:9408` and runs all 22 TPC-H queries plus a 4-scenario test module (lifecycle / data / wrong-password / ADBC pass-through). The phase also performs structural cleanup: dead `lib/` files left behind by Phase 1 (`lib/docker_backends.py`, `lib/starrocks.py`, `lib/tls.py`, `lib/driver_registry.py`) are deleted, the hand-rolled SF1 data generator is replaced with the canonical DuckDB `tpch` extension (~10s SF1 instead of ~2 min), and the per-backend TPC-H query duplicates (`queries/mysql/03-q*.sql`, `queries/postgres/03-q*.sql`) are consolidated into a single canonical home at `queries/tpch/q01.sql..q22.sql` with template-substituted `{catalog}.{db}` placeholders. The existing sqlflite FlightSQL path (`sr-flightsql`, `sr-flightsql-tls`, `tests/test_flightsql.py`) coexists unchanged.

**Requirements:** FS-SR-01, FS-SR-02, FS-SR-03, FS-SR-04, FS-SR-05, FS-SR-06, FS-SR-07, FS-SR-08, FS-SR-09
**Depends on:** Phase 3 (transitively); Phase 2 (SF1 CSV generator and bind-mount pattern)

**Success Criteria** (what must be TRUE):
  1. After `docker compose up -d`, `docker compose ps sr-external` reports `Up (healthy)` within `start_period` (~180s) and a sr-main-side `SHOW DATABASES FROM sr_flightsql_starrocks` lists a `tpch` database with all 8 TPC-H tables populated to canonical SF1 row counts (region=5, nation=25, supplier=10000, part=200000, partsupp=800000, customer=150000, orders=1500000, lineitem=6001215).
  2. From sr-main, `CREATE EXTERNAL CATALOG sr_flightsql_starrocks PROPERTIES("type"="adbc", "driver_url"="...", "uri"="grpc://sr-external:9408", "username"="root", "password"="")` succeeds.
  3. `pytest tests/test_flightsql_starrocks.py -v` exits 0 with all 4 scenarios (lifecycle, data, wrong-password, passthrough) passing.
  4. All 22 canonical TPC-H queries pass against `sr_flightsql_starrocks` (the 5 working postgres queries q04/q12/q13/q16/q21 also pass against `sr_postgres`; all 22 still pass against `sr_mysql`) — driven by `pytest tests/test_queries.py -v`.
  5. The full pre-existing test suite continues to pass with no regression — including `tests/test_flightsql.py` (5 tests) against the unchanged `sr-flightsql` / `sr-flightsql-tls` services. The 35-pytest-test baseline holds.
  6. `lib/` directory contains exactly two files — `__init__.py` and `catalog_helpers.py`. The four retired files (`docker_backends.py`, `starrocks.py`, `tls.py`, `driver_registry.py`) are deleted from disk and removed from any import.
  7. `docker/generate-sf1-data.py` is ≤50 lines and uses the DuckDB `tpch` extension (`INSTALL tpch; LOAD tpch; CALL dbgen(sf=1); COPY <table> TO ...`). A from-scratch run completes in ≤30s on a developer laptop and produces deterministic SF1 CSVs (LF line endings, with header row required by all consumers).
  8. `queries/tpch/q01.sql..q22.sql` exist as the single canonical TPC-H source, using `{catalog}.{db}` template placeholders. The per-backend duplicates `queries/mysql/03-q*.sql` and `queries/postgres/03-q*.sql` no longer exist on disk.
  9. `./run-verify.py docker/starrocks-fe_*.deb docker/starrocks-be_*.deb` completes the full ship→verify→retest cycle, including waiting for `sr-external` in the `_wait_for_healthy()` loop before launching pytest, and prints `✓ PASSED`.

**Plans:** 5 plans

Plans:
- [ ] 04-01: sr-external Compose service + TPC-H schema + SF1 init scripts — `docker/docker-compose.yml` (new `sr-external` service with `build: .`, `:ro` SF1 mount, healthcheck, `start_period: 180s`), `docker/init/sr-external/01-schema.sql` (TPC-H DDL with auto-pick keys per RESEARCH Pitfall 1), `docker/init/sr-external/02-data.sql` (single file with 8 TRUNCATE + INSERT FROM FILES statements). **(FS-SR-01, FS-SR-02, FS-SR-03)**
- [ ] 04-02: Catalog tests + canonical query loader wiring — `tests/test_flightsql_starrocks.py` (4 scenarios cloned from `test_flightsql.py` minus TLS), `tests/test_queries.py` (extend `CATALOG_MAP` with `sr_flightsql_starrocks`, add `sr_flightsql_starrocks_cat` session fixture, add `CANONICAL_BACKENDS` + `CANONICAL_SKIPS` per `04-CANONICAL-SPEC.md`). **(FS-SR-04, FS-SR-05, FS-SR-06)**
- [ ] 04-03: Integration + docs — `run-verify.py` `_wait_for_healthy()` services dict (add `'sr-external': False`), `.planning/REQUIREMENTS.md` (add FS-SR-01..09 rows + traceability), `.planning/ROADMAP.md` (this entry's Goal / Requirements / Success Criteria / plan list / Progress row), `CLAUDE.md` (Project Layout updates + Phase 4 subsection + 2 new pitfall callouts + Retired-table deletion + Phase 2 SF1 paragraph re-pointed at DuckDB tpch). **(traceability + docs only — no FS-SR-* implementation)**
- [ ] 04-04: Dead `lib/` cleanup — delete `lib/docker_backends.py`, `lib/starrocks.py`, `lib/tls.py`, `lib/driver_registry.py`; verify nothing in `tests/`, `conftest.py`, `lib/catalog_helpers.py`, or `run-verify.py` imports them; the 35-test baseline + new Phase 4 tests still pass. **(FS-SR-07)**
- [ ] 04-05: Canonical TPC-H + DuckDB SF1 generator — rewrite `docker/generate-sf1-data.py` against the DuckDB `tpch` extension (≤50 LOC, deterministic, LF/no-header CSVs), create `queries/tpch/q01.sql..q22.sql` with `{catalog}.{db}` placeholders + per-backend `-- Expected (backend): N rows` comments per `04-CANONICAL-SPEC.md`, delete `queries/mysql/03-q*.sql` and `queries/postgres/03-q*.sql`, run the calibration step (down -v && up && record actual row counts) to populate the Expected lines. **(FS-SR-08, FS-SR-09)**
```

**Edit 2 — Update the `## Progress` table at lines 35-39 by adding a row for Phase 4. The existing rows end at line 39 with the Phase 3 row. Append after the Phase 3 row:**

```markdown
| 4. FlightSQL TPC-H Against External StarRocks | 0/5 | Pending | — |
```

**Edit 3 — Update the execution-order note at lines 32-33. Replace exactly:**

```markdown
**Execution Order:**
Phase 1 executes plans sequentially: 01-01 → 01-02 → 01-03
```

**With:**

```markdown
**Execution Order:**
Phase 1: 01-01 → 01-02 → 01-03 (sequential).
Phase 4: 04-04 + 04-05 (wave 1, parallel — independent file scopes) → 04-01 (wave 2, depends on 04-05 for canonical CSVs) → 04-02 + 04-03 (wave 3, depends on 04-01 + 04-05 and 04-01 + 04-04 + 04-05 respectively).
```

**Do NOT modify:** Phases 1, 2, 3 sections; the `## Overview` block; the `## Phases` checklist at line 9 (one-line summary). The original Phase 4 placeholder bullet `- [ ] TBD (run /gsd-plan-phase 4 to break down)` at line 88 is replaced by the five concrete plan rows in Edit 1 above.
  </action>

  <acceptance_criteria>
- `grep -q "Goal:\*\* Add a second StarRocks instance" .planning/ROADMAP.md` succeeds (real goal text present).
- `! grep -q "\[To be planned\]" .planning/ROADMAP.md` succeeds (no remaining placeholder anywhere).
- `! grep -q "TBD (run /gsd-plan-phase 4 to break down)" .planning/ROADMAP.md` succeeds (placeholder line gone).
- `grep -A 30 "### Phase 4:" .planning/ROADMAP.md | grep -q "FS-SR-01, FS-SR-02, FS-SR-03, FS-SR-04, FS-SR-05, FS-SR-06, FS-SR-07, FS-SR-08, FS-SR-09"` succeeds.
- `grep -A 60 "### Phase 4:" .planning/ROADMAP.md | grep -q "Success Criteria"` succeeds.
- `awk '/### Phase 4:/,/^### Phase /' .planning/ROADMAP.md | grep -cE '^  [1-9]\. '` returns `9` (exactly 9 numbered Success Criteria).
- `grep -A 80 "### Phase 4:" .planning/ROADMAP.md | grep -q "04-01:"` succeeds.
- `grep -A 80 "### Phase 4:" .planning/ROADMAP.md | grep -q "04-02:"` succeeds.
- `grep -A 80 "### Phase 4:" .planning/ROADMAP.md | grep -q "04-03:"` succeeds.
- `grep -A 80 "### Phase 4:" .planning/ROADMAP.md | grep -q "04-04:"` succeeds.
- `grep -A 80 "### Phase 4:" .planning/ROADMAP.md | grep -q "04-05:"` succeeds.
- `grep -A 80 "### Phase 4:" .planning/ROADMAP.md | grep -q "5 plans"` succeeds.
- `grep -q "| 4\. FlightSQL TPC-H" .planning/ROADMAP.md` succeeds (Progress table row added).
- `grep -q "Phase 4: 04-01 + 04-04 + 04-05" .planning/ROADMAP.md` succeeds (execution-order note updated).
- `grep -q "DuckDB \`tpch\` extension" .planning/ROADMAP.md` succeeds (Goal mentions the new generator).
- `grep -q "queries/tpch/q01.sql..q22.sql" .planning/ROADMAP.md` succeeds (Goal/SC mention canonical home).
  </acceptance_criteria>
</task>

<task id="04-03-T03" type="auto">
  <title>Add 'sr-external' to run-verify.py:_wait_for_healthy() services dict</title>

  <read_first>
    - `run-verify.py` lines 181-189 (the `_wait_for_healthy()` services dict)
    - `04-RESEARCH.md` Pitfall 7, lines 481-491 (rationale for the edit, "five-line edit," belt-and-suspenders against `sr_flightsql_starrocks_cat` fixture race)
    - `04-PATTERNS.md` lines 476-509 (analog: the existing services dict, with the exact insert position called out — between `sr-flightsql-tls` and `sr-main`, matching the natural `depends_on` ordering per D-04)
  </read_first>

  <action>
**Single-file edit. Replace lines 181-189 of `run-verify.py` exactly:**

**Existing (lines 181-189):**
```python
def _wait_for_healthy() -> bool:
    deadline = time.monotonic() + HEALTHCHECK_TIMEOUT
    services = {
        "sr-mysql": False,
        "sr-postgres": False,
        "sr-flightsql": False,
        "sr-flightsql-tls": False,
        "sr-main": False,
    }
    reported: set[str] = set()
```

**New (one-line insertion between `sr-flightsql-tls` and `sr-main`):**
```python
def _wait_for_healthy() -> bool:
    deadline = time.monotonic() + HEALTHCHECK_TIMEOUT
    services = {
        "sr-mysql": False,
        "sr-postgres": False,
        "sr-flightsql": False,
        "sr-flightsql-tls": False,
        "sr-external": False,
        "sr-main": False,
    }
    reported: set[str] = set()
```

**Why between `sr-flightsql-tls` and `sr-main`:** The existing dict is loosely ordered by dependency depth — backends first, sr-main (which `depends_on` everything else) last. sr-external is a new backend that sr-main also `depends_on` per CONTEXT D-04, so it slots in just before `sr-main`. This is the position recommended by RESEARCH Pitfall 7 and `04-PATTERNS.md` lines 491-503.

**No other change to `run-verify.py`:**
- The ready-check at line 217 (`ready = health == "healthy" or (health == "" and state == "running")`) already handles healthchecked services correctly — sr-external has a healthcheck per 04-01, so it will report `health == "healthy"` once ready.
- `HEALTHCHECK_TIMEOUT = 300` at line 29 comfortably accommodates sr-external's `start_period: 180s`.
- The print-on-flip block at lines 219-223 needs no change — it already prints `✓ <name> healthy (<elapsed>s)` for any service that flips green, so `sr-external` will be auto-announced.

**Do NOT do:**
- Do NOT remove or reorder the existing 5 services.
- Do NOT change `HEALTHCHECK_TIMEOUT` or `HEALTHCHECK_POLL_INTERVAL` constants.
- Do NOT add sr-external to other places in run-verify.py — `_print_service_status` does not need it; only the wait loop hardcodes the service list.
- Do NOT modify the ready-check logic at line 217.
  </action>

  <acceptance_criteria>
- `grep -q "'sr-external': False" run-verify.py` succeeds.
- `grep -B 1 "'sr-external': False" run-verify.py | grep -q "'sr-flightsql-tls': False"` succeeds (correct insert position — sr-external comes immediately after sr-flightsql-tls).
- `grep -A 1 "'sr-external': False" run-verify.py | grep -q "'sr-main': False"` succeeds (sr-main still follows immediately).
- `awk '/services = \{/,/}/' run-verify.py | grep -cE '"sr-(mysql|postgres|flightsql|flightsql-tls|external|main)": False,'` returns `6` (was 5, now 6).
- `python3 -c "import ast; ast.parse(open('run-verify.py').read())"` exits 0 (file still parses).
- `python3 run-verify.py --help 2>&1 | grep -q "ship"` succeeds (script still runs, no syntax error).
- For every existing service: `for s in sr-mysql sr-postgres sr-flightsql sr-flightsql-tls sr-main; do grep -q "\"$s\": False" run-verify.py; done` succeeds (existing entries preserved).
  </acceptance_criteria>
</task>

<task id="04-03-T04" type="auto">
  <title>Update CLAUDE.md — Project Layout, Phase 2 SF1 paragraph, new Phase 4 subsection, two pitfall callouts, delete Retired table</title>

  <read_first>
    - `CLAUDE.md` lines 40-76 (Project Layout block — `tests/` listing where `test_flightsql_starrocks.py` slots in, `queries/` listing where `tpch/` slots in and `postgres/` + `mysql/` mentions get updated)
    - `CLAUDE.md` lines 112-118 (Phase 2 subsection — analog format AND target of the DuckDB tpch update)
    - `CLAUDE.md` lines 119-189 (Pitfalls block — where the two new callouts slot in; existing Pre-flight + ownership-trap entries are the analog format)
    - `CLAUDE.md` lines 191-199 (the `## Retired` table to be deleted in full)
    - `04-RESEARCH.md` Pitfall 1 lines 387-395 (DUPLICATE KEY auto-pick rationale to LINK to, not duplicate)
    - `04-RESEARCH.md` Pitfall 7 lines 481-491 (run-verify.py service dict rationale — already covered by Task 04-03-T03; CLAUDE.md just needs a brief pointer)
    - `04-CANONICAL-SPEC.md` (provides the exact `queries/tpch/` shape and the `CANONICAL_SKIPS["postgres"]` constant name to mention)
  </read_first>

  <action>
**Edit 1 — Project Layout `queries/` listing. Locate lines 57-61 of CLAUDE.md. Replace exactly:**

```
queries/                # Externalized SQL files (auto-discovered by test_queries.py)
  postgres/             # 2 TPC-H queries (select, join) — 22 planned in Phase 2
  mysql/                # 2 TPC-H queries (select, join) — 22 planned in Phase 2
  sqlite/, duckdb/, flightsql/  # 2 queries each
  cross-join/           # Cross-driver federation queries
```

**With (Phase 4 makes `queries/tpch/` the single canonical home; per-backend `03-q*.sql` files are gone):**

```
queries/                # Externalized SQL files (auto-discovered by test_queries.py)
  tpch/                 # 22 canonical TPC-H queries (q01..q22) with {catalog}.{db} placeholders — Phase 4 single source of truth
  postgres/             # 2 TPC-H queries (select, join) only
  mysql/                # 2 TPC-H queries (select, join) only
  sqlite/, duckdb/, flightsql/  # 2 queries each
  cross-join/           # Cross-driver federation queries
```

**Edit 2 — Project Layout `tests/` listing. Locate lines 62-70 of CLAUDE.md (the `tests/` block in the layout fence). Replace exactly:**

```
tests/
  test_queries.py       # Executes every .sql under queries/ against live catalogs
  test_sqlite.py        # 6 tests — catalog lifecycle, data, errors
  test_duckdb.py        # 4 tests — lifecycle, data, entrypoint, passthrough
  test_mysql.py         # 5 tests — lifecycle, data, SHOW TABLES, errors, cross-join
  test_postgres.py      # 6 tests — lifecycle, data, SHOW TABLES, errors, TLS, passthrough
  test_flightsql.py     # 5 tests — lifecycle, data, TLS, auth, passthrough
  test_cross_join.py    # 2 tests — cross-driver JOINs
  test_negative.py      # 7 tests — validation error paths
```

**With (one new line for `test_flightsql_starrocks.py`, inserted right after `test_flightsql.py` to keep the FlightSQL pair adjacent; comment columns widened to align):**

```
tests/
  test_queries.py             # Executes canonical queries/tpch/ × per-backend product + per-driver dir queries
  test_sqlite.py              # 6 tests — catalog lifecycle, data, errors
  test_duckdb.py              # 4 tests — lifecycle, data, entrypoint, passthrough
  test_mysql.py               # 5 tests — lifecycle, data, SHOW TABLES, errors, cross-join
  test_postgres.py            # 6 tests — lifecycle, data, SHOW TABLES, errors, TLS, passthrough
  test_flightsql.py           # 5 tests — sqlflite path: lifecycle, data, TLS, auth, passthrough
  test_flightsql_starrocks.py # 4 tests — external StarRocks Arrow Flight: lifecycle, data, wrong-password, passthrough (Phase 4)
  test_cross_join.py          # 2 tests — cross-driver JOINs
  test_negative.py            # 7 tests — validation error paths
```

**Edit 3 — Project Layout `lib/` listing. Locate lines 71-73 of CLAUDE.md. Replace exactly:**

```
lib/
  catalog_helpers.py    # create_adbc_catalog(), drop_catalog(), execute_sql()
  driver_registry.py    # Reads driver .so paths from ~/.config/adbc/drivers/*.toml
```

**With (Phase 4 deletes `driver_registry.py`; only `catalog_helpers.py` + `__init__.py` remain):**

```
lib/
  catalog_helpers.py    # create_adbc_catalog(), drop_catalog(), execute_sql() — only file under lib/ post-Phase-4 cleanup
```

**Edit 4 — Replace the Phase 2 SF1 subsection. Locate lines 112-117 of CLAUDE.md (the entire `## Phase 2: SF1 Data` section). Replace exactly:**

```markdown
## Phase 2: SF1 Data

Phase 2 replaces the TPC-H seed data (5 rows/table) in PostgreSQL and MySQL with full SF1 data (~900 MB CSV, 8M+ rows):
- `docker/generate-sf1-data.py` — generates SF1 CSV files (8 tables) to `docker/data/sf1/`. **Run this before first `docker compose up`** — init scripts mount the directory and fail if it's empty.
- Init scripts use `\COPY` (PostgreSQL, via psql) and `LOAD DATA INFILE` (MySQL) for bulk loading.
- 44 TPC-H query files (22 per backend) in `queries/postgres/` and `queries/mysql/`. The mysql versions all pass; 17 of the 22 postgres ones are skipped via `-- Skip:` directive — see `.planning/phases/02-*/02-NOTES-postgres-numeric.md` for the StarRocks-side fix that re-enables them.
```

**With (Phase 4 swaps the generator and consolidates the queries):**

```markdown
## Phase 2: SF1 Data

Phase 2 replaces the TPC-H seed data (5 rows/table) in PostgreSQL and MySQL with full SF1 data (~900 MB CSV, 8M+ rows). Phase 4 then swaps the generator implementation and consolidates the per-backend query duplicates into a single canonical home:
- `docker/generate-sf1-data.py` — generates SF1 CSV files (8 tables) to `docker/data/sf1/` using the canonical DuckDB `tpch` extension (`INSTALL tpch; LOAD tpch; CALL dbgen(sf=1); COPY <table> TO '<path>' (FORMAT CSV, HEADER TRUE, DELIMITER ',')`). **Generation is fast (≤30s for the full SF1) — no more multi-minute hand-rolled mock generator.** **Run this before first `docker compose up`** — init scripts mount `docker/data/sf1/` and fail if it's empty.
- Init scripts use `\COPY` (PostgreSQL, via psql) and `LOAD DATA INFILE` (MySQL) for bulk loading.
- TPC-H queries live as 22 canonical files under `queries/tpch/q01.sql..q22.sql` with `{catalog}.{db}` template placeholders, substituted at test-collection time. The per-backend skip manifest (e.g. `CANONICAL_SKIPS["postgres"]` for the postgres-numeric Arrow gap) lives at the top of `tests/test_queries.py` — see `.planning/phases/02-*/02-NOTES-postgres-numeric.md` for the StarRocks-side fix that will re-enable the 17 postgres skips. The legacy `queries/postgres/03-q*.sql` and `queries/mysql/03-q*.sql` duplicates were retired in Phase 4.
```

**Edit 5 — Insert a new "Phase 4: External StarRocks Arrow Flight" subsection. Locate the end of the (now-rewritten) `## Phase 2: SF1 Data` block. After Edit 4 lands, the Phase 2 section ends with the line beginning `... The legacy queries/postgres/03-q*.sql and queries/mysql/03-q*.sql duplicates were retired in Phase 4.`. Insert this entire block IMMEDIATELY after that line, BEFORE the blank line that precedes `## Pitfalls (...)`:**

```markdown

## Phase 4: External StarRocks Arrow Flight

Phase 4 adds a second StarRocks instance (`sr-external`) to the Compose stack and exercises the StarRocks-native Arrow Flight server end-to-end:
- `sr-external` reuses the same `docker/Dockerfile` and `.deb` as sr-main; no host ports published. Reachable from sr-main via Docker DNS at `sr-external:9408` (FE Arrow Flight).
- TPC-H schema and SF1 data are loaded into native StarRocks tables under the `tpch` database via `INSERT INTO tpch.<table> SELECT * FROM FILES('file:///opt/starrocks/data/sf1/<table>.csv', 'format'='csv', ...)`. The SF1 CSVs (now generated by the DuckDB `tpch` extension — see Phase 2 above) feed both backends.
- A new ADBC catalog `sr_flightsql_starrocks` connects from sr-main to `grpc://sr-external:9408` with `username=root` / `password=""`. All 22 canonical TPC-H queries (`queries/tpch/q01..q22.sql`) target it via the `flightsql-starrocks` backend in `tests/test_queries.py`'s `CANONICAL_BACKENDS` mapping.
- `tests/test_flightsql_starrocks.py` covers 4 scenarios (lifecycle, data, wrong-password, ADBC pass-through); the existing sqlflite path (`sr-flightsql`, `sr-flightsql-tls`, `tests/test_flightsql.py`) coexists unchanged.
- Cleanup: dead `lib/` files left over from Phase 1 (`docker_backends.py`, `starrocks.py`, `tls.py`, `driver_registry.py`) are deleted; `lib/` retains only `__init__.py` and `catalog_helpers.py`.

```

**Edit 6 — Add two new pitfall entries inside the existing `## Pitfalls (...)` block. Insert at the end of the Pitfalls section, AFTER the `### Skip directive in query files` entry (which ends with `... Removing the line re-enables the test.`) and BEFORE the `## Retired` heading. Insert these as the last two `### ...` subsections of the Pitfalls block:**

```markdown

### StarRocks DUPLICATE KEY column ordering (Phase 4)

`CREATE TABLE ... DUPLICATE KEY(col_a, col_d) DISTRIBUTED BY HASH(...)` fails with `Key columns must be the first few columns of the schema` if the named columns aren't a contiguous prefix of the schema declaration order. **Do NOT add `DUPLICATE KEY` to the sr-external TPC-H DDL.** StarRocks auto-picks the first 1-3 columns (e.g., `(l_orderkey, l_partkey, l_suppkey)` for lineitem), which is correct for these query patterns. Full rationale: `.planning/phases/04-*/04-RESEARCH.md` Pitfall 1.

### `run-verify.py` service dict (Phase 4)

`run-verify.py:_wait_for_healthy()` has a hardcoded `services = {...}` dict around line 183 that gates pytest launch on every listed service flipping green. **Adding a new compose service requires adding it to this dict** — otherwise the runner returns success before the new service is healthy and pytest fires too early, surfacing as `Database 'tpch' doesn't exist` on first run / passes on retry. sr-external is included as of Phase 4. Full rationale: `.planning/phases/04-*/04-RESEARCH.md` Pitfall 7.
```

**Edit 7 — DELETE the entire `## Retired` table. Locate lines 191-199 of CLAUDE.md (the `## Retired` heading and its 5-row table, ending with `| STARROCKS_HOME env var | Replaced by STARROCKS_HOST/STARROCKS_PORT |`). Delete this entire block including the heading. The file should end after Edit 6's `### \`run-verify.py\` service dict (Phase 4)` block — delete the blank line before `## Retired` as well so the file ends cleanly with a single trailing newline.

**Why delete the table:** It existed because the four `lib/` files (`docker_backends.py`, `starrocks.py`, `tls.py`) were left on disk after Phase 1 even though no code imported them. `STARROCKS_HOME` was an env var, never a file. Phase 4's plan 04-04 actually deletes those files from disk. Once they're deleted, the table becomes a list of things-that-no-longer-exist — pure historical noise. Future-readers can `git log` if they want history.

**Do NOT edit:** `## Quick Commands`, `## Prerequisites`, `## Architecture`, `## Test Pattern`, `## test_queries.py Conventions`, `## Key Rules`. The "35 pytest tests" line in `## Key Rules` (line 109) stays — the line says "35 pytest tests to pass, plus all query file tests" which is still accurate (the "plus" handles the new queries; 4 new tests in a new module are an additive concern, documented in the new Phase 4 section).

**Critical decisions baked in:**
- New Phase 4 subsection follows the same shape as Phase 2 — short paragraph + bullet list, no heavy code blocks (those already live in 04-RESEARCH.md / 04-PATTERNS.md).
- The two new pitfalls LINK to RESEARCH.md rather than duplicate. Project convention (per `04-PATTERNS.md`): keep CLAUDE.md tight; pitfalls beyond a one-paragraph callout live in the phase docs.
- DUPLICATE KEY pitfall is included even though it's an init-time concern, not a runtime one — because the next time a contributor edits `docker/init/sr-external/01-schema.sql` they need to know.
- run-verify.py service dict pitfall is included as a forward-warning for future Phase 5+ services.
- The `## Retired` table is deleted, not just emptied — empty tables in CLAUDE.md are confusing noise.
- Phase 2 SF1 paragraph is rewritten in-place (not added to) so the file isn't double-truth (old and new generator both described).
- Excluded from CLAUDE.md inline pitfalls (kept in RESEARCH.md only): start_period sizing math (Pitfall 3 — too detailed), Arrow Flight FE-vs-BE proxy (Pitfall 5 — only relevant if the build changes), wrong-password test path (Pitfall 4 — test author concern, not runtime).
  </action>

  <acceptance_criteria>
- `grep -q "test_flightsql_starrocks.py" CLAUDE.md` succeeds.
- `grep -q "## Phase 4: External StarRocks Arrow Flight" CLAUDE.md` succeeds.
- `grep -q "queries/tpch/" CLAUDE.md || grep -q "tpch/" CLAUDE.md` succeeds (canonical home listed).
- `grep -q "### StarRocks DUPLICATE KEY column ordering (Phase 4)" CLAUDE.md` succeeds.
- ``grep -q "### \`run-verify.py\` service dict (Phase 4)" CLAUDE.md`` succeeds.
- ``grep -q "04-RESEARCH.md\` Pitfall 1" CLAUDE.md`` succeeds (DUPLICATE KEY entry links rather than duplicates).
- ``grep -q "04-RESEARCH.md\` Pitfall 7" CLAUDE.md`` succeeds (run-verify.py entry links rather than duplicates).
- `grep -B 2 "test_flightsql_starrocks.py" CLAUDE.md | grep -q "test_flightsql.py"` succeeds (StarRocks listing immediately follows sqlflite — pair stays adjacent).
- `grep -c "^## Phase " CLAUDE.md` returns `2` (Phase 2 + Phase 4 subsections both exist).
- `awk '/^## Phase 4:/,/^## Pitfalls/' CLAUDE.md | grep -q "sr-external:9408"` succeeds (the new section mentions the catalog URI).
- `awk '/^## Phase 4:/,/^## Pitfalls/' CLAUDE.md | grep -qE "(coexist|untouched|unchanged)"` succeeds (D-12 coexistence note included).
- `awk '/^## Phase 4:/,/^## Pitfalls/' CLAUDE.md | grep -q "lib/"` succeeds (cleanup noted).
- `grep -c "^## Retired" CLAUDE.md` returns `0` (Retired table deleted).
- `! grep -q "lib/docker_backends.py" CLAUDE.md` succeeds (was only mentioned in Retired table; gone with it).
- `! grep -q "STARROCKS_HOME env var" CLAUDE.md` succeeds (was only mentioned in Retired table; gone with it).
- `! grep -q "driver_registry.py" CLAUDE.md` succeeds (lib/ listing also updated to drop it per Edit 3).
- `grep -q "DuckDB \`tpch\` extension" CLAUDE.md` succeeds (Phase 2 paragraph updated).
- `! grep -q "Lineitem generation takes ~2 minutes" CLAUDE.md` succeeds (old slow-generator wording gone — note: this lives in the Pitfalls/Pre-flight block; the Phase 2 SF1 *subsection* update is the primary narrative replacement, but if the Pre-flight bullet survives unchanged, that is acceptable as a separate "regenerating the CSVs is still a real operation" warning. Only the Phase 2 SF1 subsection's claim about ~2 min duration must be replaced.).
- `grep -q "CANONICAL_SKIPS" CLAUDE.md` succeeds (Phase 2 paragraph mentions the new skip-manifest constant by name).
- Existing baseline pitfall preserved: `grep -q "CSV line endings" CLAUDE.md` succeeds.
- Existing sections preserved: `grep -q "## Quick Commands" CLAUDE.md && grep -q "## Architecture" CLAUDE.md && grep -q "## Key Rules" CLAUDE.md` succeeds.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run these commands sequentially after all four tasks complete to confirm the integration is consistent:

```bash
# 1. REQUIREMENTS.md has all 9 new IDs + traceability + updated tally
grep -c "^| FS-SR-0[1-9] |" .planning/REQUIREMENTS.md          # expect 9
grep -q "v1 requirements: 26 total" .planning/REQUIREMENTS.md
grep -q "### FlightSQL → External StarRocks (Phase 4)" .planning/REQUIREMENTS.md
grep -c "^\*\*FS-SR-0[1-9]$" .planning/REQUIREMENTS.md         # expect 9 (multi-line bold preserved)

# 2. ROADMAP.md Phase 4 has real content + 9 success criteria + 5 plans + Progress row
! grep -q "\[To be planned\]" .planning/ROADMAP.md
! grep -q "TBD (run /gsd-plan-phase 4 to break down)" .planning/ROADMAP.md
grep -A 30 "### Phase 4:" .planning/ROADMAP.md | grep -q "FS-SR-01"
grep -A 30 "### Phase 4:" .planning/ROADMAP.md | grep -q "FS-SR-09"
awk '/### Phase 4:/,/^### Phase /' .planning/ROADMAP.md | grep -cE '^  [1-9]\. '   # expect 9
grep -A 80 "### Phase 4:" .planning/ROADMAP.md | grep -E "04-0[1-5]:" | wc -l       # expect 5
grep -q "| 4\. FlightSQL TPC-H" .planning/ROADMAP.md             # progress table row

# 3. run-verify.py services dict includes sr-external in correct slot
grep -q "'sr-external': False" run-verify.py
grep -B 1 "'sr-external': False" run-verify.py | grep -q "'sr-flightsql-tls': False"
grep -A 1 "'sr-external': False" run-verify.py | grep -q "'sr-main': False"
python3 -c "import ast; ast.parse(open('run-verify.py').read())"

# 4. CLAUDE.md has new test entry, queries/tpch/, Phase 4 section, pitfalls, Retired deleted
grep -q "test_flightsql_starrocks.py" CLAUDE.md
grep -q "tpch/" CLAUDE.md
grep -q "## Phase 4: External StarRocks Arrow Flight" CLAUDE.md
grep -q "### StarRocks DUPLICATE KEY column ordering (Phase 4)" CLAUDE.md
grep -q "### \`run-verify.py\` service dict (Phase 4)" CLAUDE.md
grep -B 2 "test_flightsql_starrocks.py" CLAUDE.md | grep -q "test_flightsql.py"
[ "$(grep -c '^## Retired' CLAUDE.md)" = "0" ]
grep -q "DuckDB \`tpch\` extension" CLAUDE.md

# 5. End-to-end (after 04-01 + 04-02 + 04-04 + 04-05 also land): the runner waits on sr-external
# Live verification only — runs against the docker stack:
# ./run-verify.py docker/starrocks-fe_*.deb docker/starrocks-be_*.deb
# Expect: line "✓ sr-external healthy (NNs)" appears before "✓ All services healthy"
```
</verification>

<must_haves>
- [ ] `run-verify.py:_wait_for_healthy()` waits for sr-external before running pytest (verifiable by reading the dict literal).
- [ ] `.planning/REQUIREMENTS.md` contains FS-SR-01 through FS-SR-09 rows with descriptions + 9 traceability rows mapping to plans 04-01..04-05.
- [ ] `.planning/ROADMAP.md` Phase 4 entry has filled `Goal:`, `Requirements:`, 9 numbered Success Criteria, and a 5-plan list — no `[TBD]` or `[To be planned]` remains in the Phase 4 section.
- [ ] `CLAUDE.md` Project Layout lists `test_flightsql_starrocks.py` under tests/, `queries/tpch/` under queries/, and trims `lib/` to only `catalog_helpers.py`.
- [ ] `CLAUDE.md` has a "Phase 4: External StarRocks Arrow Flight" sub-section, the Phase 2 SF1 paragraph is re-pointed at the DuckDB tpch generator, and the 2 new pitfalls (DUPLICATE KEY auto-pick + run-verify.py service dict) are present and link to 04-RESEARCH.md.
- [ ] `CLAUDE.md` `## Retired` table is deleted (no longer needed once 04-04 deletes the four files).
- [ ] `./run-verify.py docker/starrocks-fe_*.deb docker/starrocks-be_*.deb` end-to-end completes including sr-external in the healthcheck wait loop.
</must_haves>

<output>
After completion, create `.planning/phases/04-flightsql-tpc-h-queries-against-external-starrocks-with-arro/04-03-SUMMARY.md` documenting:
- Each of the 4 task edits (REQUIREMENTS rows + traceability, ROADMAP Phase 4 fill-in, run-verify.py dict insertion, CLAUDE.md updates) with before/after line counts.
- The cross-plan handoff: which FS-SR-NN rows map to which sibling plans (04-01 → FS-SR-01..03; 04-02 → FS-SR-04..06; 04-04 → FS-SR-07; 04-05 → FS-SR-08, FS-SR-09; 04-03 owns no FS-SR-NN — pure traceability + docs).
- Any deviation from the planned diff (the action sections show exact text; deviations should be flagged).
</output>
